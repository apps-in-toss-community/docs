node_modules
dist
coverage

# Docusaurus
.docusaurus
build

.claude/settings.local.json
.claude/*.lock
.claude.lock
.claude/worktrees/

.DS_Store
*.log

# Generated by `pnpm coverage:check` (see scripts/sdk-coverage.ts). The baseline
# (coverage-baseline.json) is tracked; the report regenerates on every CI run
# and is posted into the weekly drift issue.
sdk-coverage-report.md
