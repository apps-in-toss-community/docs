---
id: openURL
title: openURL
sidebar_label: openURL
description: Open an external URL.
---

import TryItLink from '@site/src/components/TryItLink';

# `openURL`

Opens the given URL. Depending on the scheme, this can open the host app's internal browser, a phone dialer (`tel:`), a mail client (`mailto:`), or another installed app via its custom scheme.

:::info Unofficial docs
This page is community-written. The SDK behavior itself is defined by the published [`@apps-in-toss/web-framework`](https://www.npmjs.com/package/@apps-in-toss/web-framework) release.
:::

## Signature

```ts
import { openURL } from '@apps-in-toss/web-framework';

declare function openURL(url: string): Promise<void>;
```

## Parameters

| Name | Type | Required | Description |
| --- | --- | :-: | --- |
| `url` | `string` | ✓ | The URL to open. Supports `https://`, `tel:`, and `mailto:` schemes. Invalid URLs may be silently ignored. |

## Returns

- `Promise<void>` — resolves once the open request is delivered.
- In the devtools mock, `window.open(url, '_blank')` is called instead.

## Permission

No permission required.

## Examples

### Minimal

```tsx
import { openURL } from '@apps-in-toss/web-framework';

await openURL('https://example.com');
```

### Realistic — a "Call support" button

```tsx
import { openURL } from '@apps-in-toss/web-framework';

export function SupportButton() {
  async function callSupport() {
    await openURL('tel:1234-5678');
  }

  return (
    <button type="button" onClick={callSupport}>
      Call customer support
    </button>
  );
}
```

## Try it live

Open the Navigation page in sdk-example and run the `openURL` card.

<TryItLink group="navigation" method="openURL" />

## Related APIs

- [`closeView`](./closeView) — close the current mini-app view.
- [`share`](./share) — open the system share sheet with a message.

## Related guides

- _(coming soon)_ Guides — "Mini-app navigation flow patterns"

## External references

- [`@apps-in-toss/web-framework`](https://www.npmjs.com/package/@apps-in-toss/web-framework) — SDK package. The actual exports are re-exported from [`@apps-in-toss/web-bridge`](https://www.npmjs.com/package/@apps-in-toss/web-bridge).
