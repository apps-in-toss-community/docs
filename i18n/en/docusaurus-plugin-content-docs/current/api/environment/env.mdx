---
image: /og/api-environment-env.png
id: env
title: env
sidebar_label: env
description: Namespace object exposing runtime environment constants such as the deployment ID.
---

import TryItLink from '@site/src/components/TryItLink';

# `env`

A namespace object holding runtime environment constants. Currently it exposes a single member, `getDeploymentId()`. Additional members may be added in future SDK releases.

:::info Unofficial docs
This page is community-written. The SDK behavior itself is defined by the published [`@apps-in-toss/web-framework`](https://www.npmjs.com/package/@apps-in-toss/web-framework) release.
:::

## Signature

```ts
import { env } from '@apps-in-toss/web-framework';

declare const env: {
  getDeploymentId: () => string;
};
```

### `env.getDeploymentId()`

Returns the unique identifier for the current mini-app deployment. The deployment ID is assigned per build and is useful for logging, debugging, and A/B test context tracking. Returns the same value as `getAppsInTossGlobals().deploymentId`.

```ts
declare function getDeploymentId(): string;
```

## Parameters

None.

## Returns

- `string` — the current deployment's unique identifier.

## Permission

No permission required — the `env` object is not bound to a `PermissionName`. For the typical permission flow used by other namespaces, see [Guides — Permissions pattern](/guides/permissions-pattern).

## Examples

### Minimal

```tsx
import { env } from '@apps-in-toss/web-framework';

const deploymentId = env.getDeploymentId();
console.log('Deployment ID:', deploymentId);
```

### Realistic — include deployment ID in error reports

```tsx
import { env } from '@apps-in-toss/web-framework';
import { useEffect } from 'react';

function App() {
  useEffect(() => {
    const deploymentId = env.getDeploymentId();

    window.addEventListener('unhandledrejection', (event) => {
      // Include the deployment ID so you know which build triggered the error.
      console.error('[error]', {
        deploymentId,
        reason: event.reason,
      });
    });
  }, []);

  return <main>{/* app content */}</main>;
}
```

## Try it live

Open the Environment page in sdk-example and run the `env.getDeploymentId` card to inspect the result.

<TryItLink group="environment" method="env" />

## Related APIs

- [`getAppsInTossGlobals`](./getAppsInTossGlobals) — returns brand globals including `deploymentId`.
- [`getOperationalEnvironment`](./getOperationalEnvironment) — returns the current execution environment (`toss` / `sandbox`).
- [`getTossAppVersion`](./getTossAppVersion) — returns the Toss app version.

## Related guides

- [Guides — Permissions pattern](/guides/permissions-pattern) — permission flow used by other namespaces (reference only; `env` doesn't require a permission).
- _(coming soon)_ Recipes — "Error tracking with deployment ID"

## External references

- [`@apps-in-toss/web-framework`](https://www.npmjs.com/package/@apps-in-toss/web-framework) — SDK package. The actual exports are re-exported from [`@apps-in-toss/web-bridge`](https://www.npmjs.com/package/@apps-in-toss/web-bridge).
- Web standard counterpart: none — deployment ID is a mini-app platform concept with no direct Web standard equivalent.
